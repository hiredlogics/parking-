node_modules
.next
out
dist
coverage
playwright-report
test-results
*.tsbuildinfo

# Environment files — never commit secrets.
.env
.env.*
!.env.example

*.log
.DS_Store

# Vercel project-link state (contains project + org IDs; keep local).
.vercel
.env*
